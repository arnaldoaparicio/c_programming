#include <stdio.h>
#include <math.h>

int main() {

    double A = sqrt(9); // square root
    double B = pow(2, 4); //Raise power (exponent)
    int C = round(3.14); // Rounding number to whole nearest whole
    int D = ceil(3.14); // Rounding up
    double E = floor(3.99); //Rounding down
    double F = fabs(-100); // Absolute value (how far number is to 0)
    double G = log(3); // logirithm of a number
    double H = sin(45); // sincose
    double I = cos(45); // cosine
    double J = tan(45); // tangent
    
    printf("\n%lf", A);
    
    return 0;
}